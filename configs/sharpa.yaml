# Separate Sharpa policy: 6 arm increments + 22 hand targets.
# Network and training defaults are not recovered experiment hyperparameters.
action_dim: 28
proprio_dim: 28
num_views: 2
observation_horizon: 2
action_horizon: 16
prediction_horizon: 8
vector_channels: 32
vector_layers: 2
vector_heads: 4
model_dim: 128
action_layers: 2
action_heads: 4
train_diffusion_steps: 1000
inference_steps: 10
beta_start: 0.0001
beta_end: 0.02
prediction_weight: 1.0
ema_decay: 0.99
